# Butler AI NEXUS — environment
# The visual shell needs none of these; they are placeholders for later wiring.

# VITE_API_BASE_URL=http://localhost:8787
# VITE_APP_NAME=Butler AI NEXUS

# --- Expo / Native app variables ---
# URL opened by the native screen when the user taps "Open OnSpace".
# Defaults to https://onspace.ai if not set.
# EXPO_PUBLIC_ONSPACE_URL=https://onspace.ai
